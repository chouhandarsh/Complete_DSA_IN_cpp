#include <iostream>
#include<algorithm>
#include <climits>
using namespace std;

int main() {
    // the right shift of two means that we will shift the binary numbers by that number
    // the formule is
    //  if we want to shift the value of n ny k so the formule comes to be
    //(n/2**k)
    int a = INT_MAX;
    cout<<a<<endl;
    int b = INT_MIN;
    cout<<b<<endl;

    
    return 0;
}