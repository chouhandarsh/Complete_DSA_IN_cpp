#include <iostream>
using namespace std;

int main() {
    // when we are swaping two numers we are required an extra veriable 
    //  but using the XOR oprator we can do the same
    // a = a^b
    //b = a^b ==> (a^b)^b ==> a
    //a = (a^b) ==> (a^b)^a ==> b


    
    return 0;
}