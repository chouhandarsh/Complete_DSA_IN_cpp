// THE MAIN LOGIC IS TO USE BINARY SEARCH AS TIME COMPLEXITY IS GIVEN TO log(n) so we will use binary search
// we find the mid element if the mid element is peak then no problem ]
// if the mid element is left(incr) then the peak